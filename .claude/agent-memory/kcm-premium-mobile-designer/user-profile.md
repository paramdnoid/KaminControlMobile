---
name: user-profile
description: Andre's profile, goals, and design collaboration preferences
metadata:
  type: user
---

Andre is the owner and primary developer of KaminControlMobile. He uses the app himself in the Kaminfeger (chimney sweep) field-work context.

- Values **premium, calm, work-focused UI** — explicitly asked for this in the first session
- Prefers quality over explanation — dive into code, don't over-explain before acting
- Expects systematic refactors: all files, all components, token consistency throughout
- Uses `npm run typecheck` + `npm run lint` as the gate before any work is "done"
