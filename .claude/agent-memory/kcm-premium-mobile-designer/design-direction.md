---
name: design-direction
description: Core design identity and direction for KaminControlMobile — what premium means for this app
metadata:
  type: project
---

KaminControlMobile is a calm, professional field-work tool for Kaminfeger (chimney sweep) professionals. Premium means confident clarity, not decoration.

**Identity**: Warm forest-green primary (`#1E4E46`) on warm cream background (`#F5F2EC`). Cards lift off the background via subtle shadow. Pure white cards against the cream creates perceived quality.

**Tone**: Calm, trustworthy, efficient. Every element serves a workflow step. No decorative chrome.

**Workflow**: search property → inspect context → create report → apply suggestions → complete/share.

**Why:** User explicitly requested "premium" refactor. The green-on-cream identity is intentional and should be preserved across future sessions.

**How to apply:** Before any UI change, ask: does this serve the chimney sweep in the field? Prefer density over decoration. Use the shadow system for depth, not gradients.
